import useAuth from '@hooks/useAuth';

const Home = () => {
  // const { user, isAuthenticated, login } = useAuth();

  // useEffect(() => {
  //   netlifyAuth.initialize(setUser);
  // });

  return (
    <div>
      <h1>Hello there</h1>
    </div>
  );
};

export default Home;
